RealtimeSTT>=0.3.0
faster-whisper>=1.0.0
httpx>=0.27.0
anthropic>=0.34.0
pynput>=1.7.6
pyperclip>=1.9.0
pystray>=0.19.5
Pillow>=10.4.0
python-dotenv>=1.0.0
pytest>=8.0.0
pyinstaller>=6.0.0

# Required by local_llm_formatter.py — must be imported before llama_cpp on
# Windows so the CUDA runtime DLLs are resolved from torch/lib/.
# Install from https://pytorch.org (select your CUDA version).
torch>=2.0.0

# Optional mic-test dependencies (used in SettingsWindow._run_mic_test).
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0

# Local GGUF cleaner (formatter_backend="local"). The cu124 prebuilt wheel is
# forward-compatible with CUDA 12.x runtimes (we use it under cu126 torch).
# Install with:
#   pip install --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124 \
#       llama-cpp-python==0.3.4
# Without the extra index, pip falls back to a CPU-only source build.
llama-cpp-python==0.3.4
